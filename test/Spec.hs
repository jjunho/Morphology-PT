{-# LANGUAGE OverloadedStrings #-}

-- import           Data.Text    (Text)
-- import qualified Data.Text    as T
-- import qualified Data.Text.IO as TIO

-- import           Test.Hspec

main :: IO ()
main = putStrLn "not yet"
-- main = hspec $ do
--   describe "mkNounM" $ do
--     it "makes a masculine noun" $ do
--       mkNounM "dia" `shouldBe` Noun [["dia","dias"],[]]

--   describe "mkNounF" $ do
--     it "makes a feminine noun" $ do
--       mkNounF "noite" `shouldBe` Noun [[],["noite","noites"]]

--   describe "mkAdj" $ do
--     it "makes an adjective" $ do
--       mkAdj "bom" `shouldBe` Adj [["bom","bons"],["boa","boas"]]

-- dia = mkNounM "dia"
-- tarde = mkNounF "tarde"
-- bom = mkAdj "bom"
-- este = mkDet "este"
-- meu = mkPoss "meu"
-- eu = mkPro "eu"
